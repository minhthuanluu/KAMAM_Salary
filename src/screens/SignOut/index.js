import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const SignOut = (props) => {
    return (
        <SafeAreaView>
            <Text>
                SignOut
            </Text>
        </SafeAreaView>
    );
}

export default SignOut;