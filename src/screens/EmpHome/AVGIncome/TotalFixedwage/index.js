import React from 'react';
import { SafeAreaView,Text } from 'react-native';
import { Header } from '../../../../comps';

const TotalFixedwage=(props)=> {
    return (
        <SafeAreaView>
            <Header title="Tổng lương cố định" />
            
        </SafeAreaView>
    );
}

export default TotalFixedwage;