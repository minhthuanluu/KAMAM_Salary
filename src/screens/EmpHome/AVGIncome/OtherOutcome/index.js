import React from 'react';
import { SafeAreaView,Text } from 'react-native';
import { Header } from '../../../../comps';

const OtherOutcome=(props)=> {
    return (
        <SafeAreaView>
            <Header title="Tổng chi khác" />
            
        </SafeAreaView>
    );
}

export default OtherOutcome;