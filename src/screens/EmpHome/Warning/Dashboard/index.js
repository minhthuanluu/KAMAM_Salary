import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const WarningDashboard = (props) => {
    return (
        <SafeAreaView>
            <Text>
                màn hình chính cảnh báo
            </Text>
        </SafeAreaView>
    );
}

export default WarningDashboard;