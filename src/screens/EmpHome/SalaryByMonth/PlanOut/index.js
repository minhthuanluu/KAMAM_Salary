import React from 'react';
import { SafeAreaView,Text } from 'react-native';

const PlanOut=(props)=> {
    return (
        <SafeAreaView>
            <Text>
            Vượt kế hoạch
            </Text>
            
        </SafeAreaView>
    );
}

export default PlanOut;