import React from 'react';
import { SafeAreaView,Text } from 'react-native';
import { Header } from '../../../../comps';

const PlanOut=(props)=> {
    return (
        <SafeAreaView>
             <Header title="Chi thương vượt KH Tháng" />
            
        </SafeAreaView>
    );
}

export default PlanOut;